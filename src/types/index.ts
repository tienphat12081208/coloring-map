export type responseNode = {
  responseData: [];
  responseSource: [];
};
export type nodeType = {
  data: {
    id: number;
    label: string;
    type: number;
  };
  position: {
    x: string;
    y: string;
  };
};

export type dataType = {
  value: string;
};
